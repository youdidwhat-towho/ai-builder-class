# properties

One card per property, kept forever, including after the deal dies. From
`_templates/property.md`.

The card is an index, not a container. It points at the deal file and the comps
rather than holding them. Next time that address shows up, you already know its
history.
