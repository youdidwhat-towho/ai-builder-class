---
name: capture
description: Append anything the user says into today's daily note with a timestamp, with no questions about where it belongs. Use whenever they dump a thought, a call recap, a number, an address, a reminder, or say "capture this", "write this down", "note that", "remember this", "add to my notes". This is the front door of the vault.
---

# Capture

One door. No decisions. This is the skill that keeps the vault alive, because a
capture system that asks a question before it accepts input is a capture system
people stop using.

## Before your first capture of a session

Read `CLAUDE.md` in the vault if you have not this session. Do not assume it was
loaded for you. It carries how this person wants to be worked with.

## What to do

Append to `daily/YYYY-MM-DD.md` for today. Create the file if it does not exist.

```markdown
## HH:MM
<what they said, cleaned up just enough to be readable later>
```

That is it. Then confirm in one short line and get out of the way.

## The rules

1. **Never ask where it should go.** Not "should this be a deal or a contact?"
   Not "which folder?" The answer is always today's daily note. Filing happens
   later, by you, not by them.
2. **Never refuse a capture.** Half an address, a name with no context, a number
   with no label. Take it. An unclear note beats a lost one.
3. **Keep their words.** Clean up transcription noise and obvious slips. Do not
   rewrite their phrasing into yours, and never summarize a capture down to a
   sentence. The detail is the value.
4. **Timestamp everything.** Use their local time.
5. **Do not create other files during a capture.** No deal card, no contact file,
   no follow-up task, unless they explicitly ask. Capture is capture.
6. **One line back.** "Got it, 2:14pm." They are mid-thought, not waiting on you.

## Filing, later

When they ask you to process the day, or at end of day, read the daily note and
propose what should become a deal, a contact, a property card or a decision.
**Propose, then do it once they say yes.** The daily note is append only, so
nothing gets moved out of it. It stays as the record of what happened when.
